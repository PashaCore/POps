# Agent Documentation

The POps Agent is a modular .NET 8 application. It consists of the core service (POpsAgent), the system tray UI (POpsTray), the screen streaming service (POpsVision), and recovery tools (POpsWatchdog, POpsUpdater).