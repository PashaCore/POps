# Backend Documentation

The POps Backend is built with Python 3.12 and FastAPI. It exposes a standard REST API for the dashboard and runs a highly-concurrent WebSocket server for real-time agent connections.